# Trained Models

This directory stores trained model weights generated during development.

Large model files are not included in this repository due to GitHub file size limitations.

The following models were developed:

- Bidirectional LSTM
- DistilBERT

To reproduce the models, run the training notebooks located in the `notebooks/` directory.
